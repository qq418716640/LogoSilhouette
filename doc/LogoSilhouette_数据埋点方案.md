# LogoSilhouette 数据埋点方案

## 目录
- [1. 概述](#1-概述)
- [2. 环境配置](#2-环境配置)
- [3. 埋点架构](#3-埋点架构)
- [4. 事件分类](#4-事件分类)
- [5. 关键指标](#5-关键指标)
- [6. 实施清单](#6-实施清单)
- [7. 数据分析建议](#7-数据分析建议)

---

## 1. 概述

### 1.1 目标
通过 Umami Analytics 实现对 LogoSilhouette 工具的全链路数据追踪,帮助我们:
- 了解用户行为和使用习惯
- 优化产品功能和用户体验
- 评估营销推广效果
- 识别技术问题和性能瓶颈

### 1.2 技术栈
- **分析平台**: Umami Cloud (https://umami-rose-delta.vercel.app)
- **实现方式**: 客户端埋点 (Browser-based)
- **数据传输**: 实时异步上报
- **隐私合规**: 不使用 Cookie,符合 GDPR/CCPA

### 1.3 设计原则
- **最小侵入**: 不影响用户体验和页面性能
- **结构化**: 统一的事件命名和参数规范
- **可扩展**: 易于添加新事件和维度
- **开发友好**: 开发环境自动打印日志便于调试

---

## 2. 环境配置

### 2.1 环境区分

项目支持多环境配置,通过环境变量区分开发和生产环境的 Umami Website ID:

| 环境 | 配置文件 | Website ID | 用途 |
|------|---------|-----------|------|
| 开发环境 | `.env.development` | `8133b90d-9f06-4b08-a1ef-9f522a35490d` | 本地开发测试 |
| 生产环境 | `.env.production` | 待配置 | 正式线上环境 |

### 2.2 配置文件

**`.env.development`** (开发环境)
```bash
VITE_UMAMI_WEBSITE_ID=8133b90d-9f06-4b08-a1ef-9f522a35490d
VITE_UMAMI_SRC=https://umami-rose-delta.vercel.app/script.js
```

**`.env.production`** (生产环境)
```bash
# TODO: 生产上线前替换为生产环境的 Website ID
VITE_UMAMI_WEBSITE_ID=your-production-website-id
VITE_UMAMI_SRC=https://umami-rose-delta.vercel.app/script.js
```

### 2.3 脚本加载

在 `index.html` 中通过环境变量动态加载:

```html
<script
  defer
  src="%VITE_UMAMI_SRC%"
  data-website-id="%VITE_UMAMI_WEBSITE_ID%"
></script>
```

Vite 构建时会自动替换为对应环境的配置。

### 2.4 本地开发

开发环境下,所有埋点事件会在控制台输出:

```javascript
[Analytics] ls_image_uploaded {
  tool: 'LogoSilhouette',
  tool_version: 'v1',
  file_type: 'image/png',
  file_size_kb: 245,
  img_w: 800,
  img_h: 600
}
```

---

## 3. 埋点架构

### 3.1 架构图

```
┌─────────────────────────────────────────────────────┐
│                    用户交互                          │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│             events.ts (事件定义层)                   │
│  - 预定义的业务事件函数                              │
│  - 参数校验和转换                                    │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│             tracker.ts (追踪器层)                    │
│  - 通用数据注入 (tool, version, page, locale, utm)  │
│  - 环境判断 (DEV 日志 / PROD 上报)                  │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│           window.umami.track() (SDK层)              │
│  - Umami 官方 JS SDK                                │
│  - 网络请求和数据上报                               │
└─────────────────┬───────────────────────────────────┘
                  │
                  ▼
┌─────────────────────────────────────────────────────┐
│              Umami Cloud 服务                        │
└─────────────────────────────────────────────────────┘
```

### 3.2 核心模块

#### 3.2.1 `tracker.ts` - 追踪器基础类

**职责**:
- 初始化通用数据 (tool, version, page, locale, UTM)
- 提供 `track()` 方法统一上报
- 开发环境日志输出

**通用数据字段**:
```typescript
{
  tool: 'LogoSilhouette',      // 工具名称
  tool_version: 'v1',           // 工具版本
  page: '/logosilhouette',      // 页面路径
  locale: 'zh-CN',              // 用户语言
  utm_source: string,           // 流量来源
  utm_medium: string,           // 流量媒介
  utm_campaign: string          // 营销活动
}
```

#### 3.2.2 `events.ts` - 事件定义层

**职责**:
- 定义所有业务事件函数
- 参数校验和格式转换
- 提供类型安全的 API

**示例**:
```typescript
export const events = {
  imageUploaded: (info: {
    fileType: string
    fileSizeKb: number
    imgW: number
    imgH: number
  }) =>
    tracker.track('ls_image_uploaded', {
      file_type: info.fileType,
      file_size_kb: info.fileSizeKb,
      img_w: info.imgW,
      img_h: info.imgH,
    }),
}
```

### 3.3 使用方式

```typescript
import { events } from '@/analytics'

// 上传图片时
events.imageUploaded({
  fileType: file.type,
  fileSizeKb: Math.round(file.size / 1024),
  imgW: img.width,
  imgH: img.height,
})

// 导出文件时
events.exportCompleted({
  format: 'svg',
  resolution: 1024,
  durationMs: 125,
  fileSizeKb: 18,
})
```

---

## 4. 事件分类

### 4.1 事件命名规范

**格式**: `ls_<模块>_<动作>`

- 前缀: `ls_` (LogoSilhouette 缩写)
- 模块: hero / upload / preset / process / export / nav / faq / page
- 动作: view / click / open / select / change / start / complete / fail

**示例**:
- `ls_hero_view` - Hero 区域曝光
- `ls_image_uploaded` - 图片上传成功
- `ls_export_click` - 点击导出按钮

### 4.2 完整事件清单

#### 4.2.1 页面级事件 (Page Level)

| 事件名 | 触发时机 | 关键参数 | 备注 |
|-------|---------|---------|------|
| `ls_page_ready` | 页面可交互 | `tt_interactive_ms` | 衡量页面加载性能 |
| `ls_app_loaded` | React 应用挂载 | `worker_supported` | 识别浏览器兼容性 |
| `ls_scroll_depth` | 滚动深度达到阈值 | `percent` (25/50/75/100) | 衡量内容阅读深度 |
| `ls_section_view` | 区块进入可视区 | `section_id` | 衡量内容曝光 |

#### 4.2.2 Hero 区事件 (Hero Section)

| 事件名 | 触发时机 | 关键参数 | 备注 |
|-------|---------|---------|------|
| `ls_hero_view` | Hero 区域曝光 | - | 首屏曝光追踪 |
| `ls_hero_upload_click` | 点击上传按钮 | `cta_id` | 主 CTA 点击 |
| `ls_hero_sample_click` | 点击示例图片 | `sample_id` | 示例图点击 |

#### 4.2.3 上传事件 (Upload Flow)

| 事件名 | 触发时机 | 关键参数 | 备注 |
|-------|---------|---------|------|
| `ls_upload_open` | 打开上传面板 | `source` (hero/app) | 上传入口追踪 |
| `ls_image_uploaded` | 文件选择上传成功 | `file_type`, `file_size_kb`, `img_w`, `img_h` | 主要上传方式 |
| `ls_image_dropped` | 拖拽上传成功 | `file_type`, `file_size_kb`, `img_w`, `img_h` | 拖拽上传追踪 |
| `ls_sample_loaded` | 加载示例图片 | `sample_id` | 示例图加载 |

**关键指标**:
- 上传成功率 = `image_uploaded` / `upload_open`
- 平均文件大小 = avg(`file_size_kb`)
- 常见图片尺寸分布

#### 4.2.4 预设和参数事件 (Settings)

| 事件名 | 触发时机 | 关键参数 | 备注 |
|-------|---------|---------|------|
| `ls_preset_selected` | 切换预设 | `preset`, `is_default` | 预设使用情况 |
| `ls_advanced_toggle` | 展开/折叠高级设置 | `state` (open/close) | 高级用户占比 |
| `ls_param_changed` | 修改单个参数 | `param`, `value` | 参数调整行为 |

**关键指标**:
- 默认预设使用率 = `is_default=true` 占比
- 高级设置打开率 = `state=open` 用户数
- 热门调参 = `param` 频次统计

#### 4.2.5 处理事件 (Processing Pipeline)

| 事件名 | 触发时机 | 关键参数 | 备注 |
|-------|---------|---------|------|
| `ls_process_started` | 开始处理 | `trigger`, `start_step` | 处理触发追踪 |
| `ls_process_completed` | 处理成功 | `duration_ms`, `fallback_no_crop` | 性能和成功率 |
| `ls_process_failed` | 处理失败 | `step`, `error_code` | 错误追踪 |
| `ls_preview_tab` | 切换预览标签 | `tab` (source/result) | 预览使用情况 |

**关键指标**:
- 处理成功率 = `completed` / `started`
- 平均处理时长 = avg(`duration_ms`)
- 失败步骤分布 = `step` 统计
- Fallback 触发率 = `fallback_no_crop=true` 占比

#### 4.2.6 SVG 质量指标 (Quality Metrics)

| 事件名 | 触发时机 | 关键参数 | 备注 |
|-------|---------|---------|------|
| `ls_svg_quality` | SVG 生成完成 | `paths_count`, `nodes_count`, `white_removed` | SVG 复杂度追踪 |

**关键指标**:
- 平均路径数 = avg(`paths_count`)
- 平均节点数 = avg(`nodes_count`)
- 白色清理率 = avg(`white_removed`)

#### 4.2.7 导出事件 (Export Flow)

| 事件名 | 触发时机 | 关键参数 | 备注 |
|-------|---------|---------|------|
| `ls_export_panel_view` | 导出面板曝光 | - | 导出意向追踪 |
| `ls_export_format` | 选择导出格式 | `format` (svg/png/jpg) | 格式偏好 |
| `ls_export_resolution` | 选择分辨率 | `resolution` | 分辨率偏好 |
| `ls_export_click` | 点击导出按钮 | `format`, `resolution`, `preset`, `has_result` | 导出意图 |
| `ls_export_started` | 开始导出 | `format`, `resolution` | 导出开始 |
| `ls_export_completed` | 导出成功 | `format`, `resolution`, `duration_ms`, `file_size_kb` | 导出成功追踪 |
| `ls_export_failed` | 导出失败 | `format`, `resolution`, `stage`, `error_code` | 导出错误追踪 |
| `ls_export_download` | 文件下载 | `format`, `resolution` | 实际下载行为 |

**关键漏斗**:
```
export_panel_view (曝光)
  ↓
export_click (意图)
  ↓
export_started (开始)
  ↓
export_completed (成功)
  ↓
export_download (下载)
```

**关键指标**:
- 导出转化率 = `export_click` / `export_panel_view`
- 导出成功率 = `export_completed` / `export_started`
- 下载完成率 = `export_download` / `export_completed`
- 格式偏好分布 = `format` 统计
- 分辨率偏好分布 = `resolution` 统计

#### 4.2.8 导航事件 (Navigation)

| 事件名 | 触发时机 | 关键参数 | 备注 |
|-------|---------|---------|------|
| `ls_nav_click` | 点击导航链接 | `location`, `link_id`, `to` | 导航使用追踪 |
| `ls_outbound_main_product` | 点击主产品链接 | `to` | 主产品导流 |
| `ls_cta_click` | 点击 CTA 按钮 | `cta_id`, `location` | CTA 效果追踪 |
| `ls_external_link` | 点击外部链接 | `to_domain` | 外链追踪 |

#### 4.2.9 FAQ 事件 (FAQ Section)

| 事件名 | 触发时机 | 关键参数 | 备注 |
|-------|---------|---------|------|
| `ls_faq_view` | FAQ 区域曝光 | - | FAQ 曝光 |
| `ls_faq_toggle` | 展开/折叠问题 | `faq_id`, `state` | FAQ 交互 |
| `ls_faq_link` | 点击 FAQ 内链接 | `faq_id`, `to` | FAQ 链接点击 |

---

## 5. 关键指标

### 5.1 核心转化漏斗

```
页面访问 (Umami 自动统计)
  ↓ 85%
Hero 曝光 (ls_hero_view)
  ↓ 40%
上传图片 (ls_image_uploaded)
  ↓ 95%
处理成功 (ls_process_completed)
  ↓ 60%
导出意图 (ls_export_click)
  ↓ 98%
导出成功 (ls_export_completed)
  ↓ 90%
文件下载 (ls_export_download)
```

**目标转化率**:
- 整体转化率 (访问 → 下载) ≥ 25%
- 上传转化率 (曝光 → 上传) ≥ 40%
- 导出转化率 (成功 → 下载) ≥ 50%

### 5.2 性能指标

| 指标 | 事件来源 | 目标值 |
|------|---------|-------|
| 页面可交互时间 | `ls_page_ready.tt_interactive_ms` | < 2000ms |
| 处理平均时长 | `ls_process_completed.duration_ms` | < 1500ms |
| 处理超时率 | `ls_process_failed` (timeout) | < 1% |
| SVG 导出时长 | `ls_export_completed.duration_ms` (svg) | < 100ms |
| PNG 导出时长 | `ls_export_completed.duration_ms` (png) | < 500ms |

### 5.3 质量指标

| 指标 | 事件来源 | 说明 |
|------|---------|------|
| 处理成功率 | `process_completed` / `process_started` | 核心成功率 |
| 导出成功率 | `export_completed` / `export_started` | 导出稳定性 |
| SVG 平均节点数 | `ls_svg_quality.nodes_count` | 输出质量 |
| 错误分布 | `process_failed.error_code` | 问题定位 |

### 5.4 用户行为指标

| 指标 | 事件来源 | 说明 |
|------|---------|------|
| 默认预设使用率 | `preset_selected.is_default=true` | 简单用户占比 |
| 高级设置使用率 | `advanced_toggle.state=open` | 高级用户占比 |
| 参数调整频次 | `param_changed` 次数 | 用户精细化程度 |
| 示例图使用率 | `sample_loaded` / `image_uploaded` | 快速体验路径 |
| 拖拽上传率 | `image_dropped` / (`image_uploaded` + `image_dropped`) | 高级交互占比 |

### 5.5 流量指标

| 指标 | 事件来源 | 说明 |
|------|---------|------|
| 流量来源分布 | `utm_source` | 渠道效果 |
| 营销活动效果 | `utm_campaign` | 活动 ROI |
| 外链点击率 | `external_link`, `outbound_main_product` | 导流效果 |
| CTA 点击率 | `cta_click` 各位置 | CTA 效果对比 |

---

## 6. 实施清单

### 6.1 已完成 ✅

- [x] 埋点基础架构 (`tracker.ts`, `events.ts`)
- [x] 50+ 事件定义完成
- [x] 开发环境日志输出
- [x] 通用数据自动注入
- [x] UTM 参数自动解析
- [x] 环境变量配置文件 (`.env.development`, `.env.production`)
- [x] Vite HTML 环境变量替换插件
- [x] index.html Umami 脚本引入

### 6.2 待实施 🚧

#### 6.2.1 组件埋点集成

需要在以下组件中集成 `events.*` 调用:

**Hero.tsx**
- [ ] `events.heroView()` - 组件挂载时
- [ ] `events.heroUploadClick(ctaId)` - 上传按钮点击
- [ ] `events.heroSampleClick(sampleId)` - 示例图点击

**ImageUploader.tsx**
- [ ] `events.uploadOpen(source)` - 上传面板打开
- [ ] `events.imageUploaded(info)` - 文件选择上传
- [ ] `events.imageDropped(info)` - 拖拽上传
- [ ] `events.sampleLoaded(sampleId)` - 示例图加载

**PresetSelector.tsx**
- [ ] `events.presetSelected(preset, isDefault)` - 预设切换

**AdvancedSettings.tsx**
- [ ] `events.advancedToggle(state)` - 高级设置展开/折叠
- [ ] `events.paramChanged(param, value)` - 参数修改

**Preview.tsx**
- [ ] `events.previewTab(tab)` - 预览标签切换

**ExportPanel.tsx**
- [ ] `events.exportPanelView()` - 面板曝光 (Intersection Observer)
- [ ] `events.exportFormat(format)` - 格式选择
- [ ] `events.exportResolution(resolution)` - 分辨率选择
- [ ] `events.exportClick(data)` - 导出按钮点击
- [ ] `events.exportStarted(format, resolution)` - 开始导出
- [ ] `events.exportCompleted(data)` - 导出成功
- [ ] `events.exportFailed(data)` - 导出失败
- [ ] `events.exportDownload(format, resolution)` - 文件下载

**processor.worker.ts / useProcessor.ts**
- [ ] `events.processStarted(trigger, startStep)` - 处理开始
- [ ] `events.processCompleted(durationMs, fallbackNoCrop)` - 处理成功
- [ ] `events.processFailed(step, errorCode)` - 处理失败
- [ ] `events.svgQuality(data)` - SVG 质量指标

**Header.tsx**
- [ ] `events.navClick(data)` - 导航链接点击
- [ ] `events.outboundMainProduct(to)` - 主产品链接点击

**FAQ.tsx**
- [ ] `events.faqView()` - FAQ 曝光
- [ ] `events.faqToggle(faqId, state)` - FAQ 展开/折叠
- [ ] `events.faqLink(faqId, to)` - FAQ 链接点击

**App.tsx / main.tsx**
- [ ] `events.pageReady(ttInteractiveMs)` - 页面可交互
- [ ] `events.appLoaded(workerSupported)` - 应用加载
- [ ] `events.scrollDepth(percent)` - 滚动深度 (25/50/75/100)
- [ ] `events.sectionView(sectionId)` - 区块曝光

#### 6.2.2 生产环境配置

- [ ] 在 Umami Cloud 创建生产环境 Website
- [ ] 将生产 Website ID 填入 `.env.production`
- [ ] 验证生产环境数据上报正常

#### 6.2.3 测试验证

- [ ] 本地开发环境测试所有事件
- [ ] 验证控制台日志输出正确
- [ ] 预发布环境验证数据上报
- [ ] 生产环境灰度验证

### 6.3 优化建议 💡

- [ ] 添加错误边界捕获和上报
- [ ] 实现用户会话 ID 追踪 (localStorage)
- [ ] 添加用户旅程关键路径分析
- [ ] 实现 A/B 测试参数传递
- [ ] 添加自定义维度 (设备类型、屏幕尺寸等)

---

## 7. 数据分析建议

### 7.1 日常监控

**每日关注指标**:
- UV / PV
- 核心转化率 (访问 → 下载)
- 处理成功率
- 导出成功率
- 平均处理时长

**监控大盘设置**:
```
┌─────────────────────────────────────┐
│  今日核心指标                        │
│  - UV: 1,245  ▲ 12%                │
│  - 转化率: 28.5%  ▼ 2.1%           │
│  - 处理成功率: 98.2%  ▲ 0.3%       │
└─────────────────────────────────────┘

┌─────────────────────────────────────┐
│  转化漏斗                            │
│  Hero 曝光        1,000  (100%)     │
│    ↓ 42%                            │
│  上传图片          420  (42%)       │
│    ↓ 96%                            │
│  处理成功          403  (40.3%)     │
│    ↓ 62%                            │
│  导出意图          250  (25%)       │
│    ↓ 98%                            │
│  导出成功          245  (24.5%)     │
│    ↓ 92%                            │
│  文件下载          225  (22.5%)     │
└─────────────────────────────────────┘
```

### 7.2 专题分析

#### 7.2.1 用户行为分析

**问题**: 用户更喜欢哪种预设?
- 查看 `ls_preset_selected.preset` 分布
- 对比不同预设的导出成功率
- 分析高级用户的参数调整模式

**问题**: 用户在哪个环节流失最多?
- 绘制完整转化漏斗
- 识别最大流失节点
- 分析流失用户的特征 (图片尺寸、格式等)

#### 7.2.2 性能分析

**问题**: 处理速度是否影响转化?
- 对比不同 `duration_ms` 区间的导出率
- 分析超时失败的图片特征
- 评估性能优化效果

#### 7.2.3 质量分析

**问题**: SVG 输出质量如何?
- 分析 `paths_count` 和 `nodes_count` 分布
- 对比不同预设的质量指标
- 识别需要优化的边缘 case

#### 7.2.4 营销分析

**问题**: 哪个渠道带来的用户质量最高?
- 按 `utm_source` 分组对比转化率
- 分析不同渠道用户的行为差异
- 评估营销活动 ROI

### 7.3 Umami Dashboard 配置建议

**自定义事件分组**:
1. **核心转化漏斗**: page_view → hero_view → image_uploaded → process_completed → export_download
2. **导出流程**: export_panel_view → export_click → export_completed → export_download
3. **错误监控**: process_failed, export_failed
4. **性能监控**: page_ready, process_completed.duration_ms, export_completed.duration_ms

**关键报表**:
- **转化漏斗报表**: 可视化完整用户旅程
- **格式偏好报表**: 各导出格式使用占比
- **错误分布报表**: 错误类型和频次统计
- **性能趋势报表**: 处理时长的时间序列
- **流量来源报表**: UTM 参数多维分析

### 7.4 数据驱动优化案例

**案例 1: 优化上传转化率**
- 发现问题: Hero 曝光 → 上传 只有 30%
- 数据分析: `hero_upload_click` 点击率正常,但 `image_uploaded` 转化低
- 猜测假设: 用户不知道支持什么格式,或选择文件后放弃
- 优化方案: 在上传按钮旁增加 "支持 PNG/JPG/SVG" 提示
- 验证效果: 上传转化率提升至 42%

**案例 2: 减少处理失败率**
- 发现问题: `process_failed` 有 5% 失败率
- 数据分析: 按 `step` 分组发现 80% 失败在 `crop` 步骤
- 猜测假设: 某些图片无法识别主体导致裁剪失败
- 优化方案: crop 失败时自动 fallback 跳过裁剪
- 验证效果: 失败率降至 1.2%

**案例 3: 提升导出完成率**
- 发现问题: `export_click` 后只有 70% 完成下载
- 数据分析: `export_failed` 很少,但 `export_download` / `export_completed` 低
- 猜测假设: 用户导出成功但不知道如何下载,或浏览器拦截
- 优化方案: 导出成功后明确提示 "文件已下载至..." + 浏览器下载权限引导
- 验证效果: 下载完成率提升至 92%

---

## 8. 附录

### 8.1 事件参数字典

| 参数名 | 类型 | 说明 | 示例值 |
|-------|------|------|-------|
| `tool` | string | 工具名称 (固定值) | `LogoSilhouette` |
| `tool_version` | string | 工具版本 | `v1` |
| `page` | string | 页面路径 | `/logosilhouette` |
| `locale` | string | 用户语言 | `zh-CN`, `en-US` |
| `utm_source` | string | 流量来源 | `google`, `facebook` |
| `utm_medium` | string | 流量媒介 | `cpc`, `social` |
| `utm_campaign` | string | 营销活动 | `launch_2024` |
| `cta_id` | string | CTA 唯一标识 | `hero_upload_btn` |
| `sample_id` | string | 示例图 ID | `nike`, `apple` |
| `source` | string | 来源位置 | `hero`, `app` |
| `file_type` | string | 文件 MIME 类型 | `image/png` |
| `file_size_kb` | number | 文件大小 (KB) | `245` |
| `img_w` | number | 图片宽度 (px) | `800` |
| `img_h` | number | 图片高度 (px) | `600` |
| `preset` | string | 预设名称 | `clean`, `minimal`, `detailed` |
| `is_default` | boolean | 是否默认预设 | `true`, `false` |
| `state` | string | 状态 | `open`, `close` |
| `param` | string | 参数名称 | `qtres`, `pathomit` |
| `value` | any | 参数值 | `2`, `5` |
| `trigger` | string | 触发来源 | `upload`, `preset`, `param` |
| `start_step` | string | 开始步骤 | `resize`, `bw`, `trace` |
| `duration_ms` | number | 耗时 (毫秒) | `1250` |
| `fallback_no_crop` | boolean | 是否跳过裁剪 | `true`, `false` |
| `step` | string | 步骤名称 | `crop`, `trace` |
| `error_code` | string | 错误代码 | `timeout`, `invalid_image` |
| `tab` | string | 标签页 | `source`, `result` |
| `format` | string | 导出格式 | `svg`, `png`, `jpg` |
| `resolution` | number | 分辨率 | `1024`, `2048` |
| `has_result` | boolean | 是否有处理结果 | `true`, `false` |
| `stage` | string | 导出阶段 | `render`, `download` |
| `paths_count` | number | SVG 路径数量 | `3` |
| `nodes_count` | number | SVG 节点数量 | `145` |
| `white_removed` | number | 清除的白色路径数 | `2` |
| `location` | string | 位置标识 | `header`, `footer` |
| `link_id` | string | 链接 ID | `nav_home` |
| `to` | string | 目标链接 | `/features` |
| `to_domain` | string | 目标域名 | `github.com` |
| `faq_id` | string | FAQ ID | `faq_1_format` |
| `percent` | number | 百分比 | `25`, `50`, `75`, `100` |
| `section_id` | string | 区块 ID | `hero`, `features` |
| `tt_interactive_ms` | number | 可交互时间 (ms) | `1800` |
| `worker_supported` | boolean | 是否支持 Worker | `true`, `false` |

### 8.2 错误代码规范

| 错误代码 | 说明 | 触发场景 |
|---------|------|---------|
| `timeout` | 处理超时 | 处理时间超过 15 秒 |
| `invalid_image` | 无效图片 | 图片无法解码 |
| `no_main_component` | 无主体 | 无法识别主体对象 |
| `crop_failed` | 裁剪失败 | 裁剪算法异常 |
| `trace_failed` | 矢量化失败 | imagetracerjs 异常 |
| `render_failed` | 渲染失败 | Canvas/SVG 渲染异常 |
| `download_failed` | 下载失败 | 浏览器下载 API 异常 |
| `unknown` | 未知错误 | 其他未分类错误 |

### 8.3 预设 ID 映射

| preset | 说明 |
|--------|------|
| `clean` | Clean Silhouette (默认) |
| `minimal` | Minimal Logo |
| `detailed` | Keep Details |

### 8.4 FAQ ID 命名

格式: `faq_<序号>_<简短主题>`

示例:
- `faq_1_format` - 支持的格式
- `faq_2_privacy` - 隐私安全
- `faq_3_quality` - 输出质量

---

## 9. 总结

本埋点方案为 LogoSilhouette 提供了**完整、结构化、可扩展**的数据追踪能力,涵盖:

✅ **完整的用户旅程**: 从访问 → 上传 → 处理 → 导出 → 下载全链路追踪
✅ **多维度数据**: 行为、性能、质量、营销四大维度
✅ **环境隔离**: 开发/生产环境独立配置,互不干扰
✅ **开发友好**: 本地自动日志,便于调试和验证
✅ **隐私合规**: 不使用 Cookie,符合 GDPR/CCPA

**下一步行动**:
1. 生产环境创建 Umami Website,替换 `.env.production` 中的 ID
2. 按照 [6.2 实施清单](#62-待实施-) 在各组件中集成埋点调用
3. 本地测试验证所有事件触发正常
4. 上线后监控核心指标,建立数据看板
5. 基于数据分析持续优化产品体验

---

**文档版本**: v1.0
**最后更新**: 2026-01-27
**维护者**: LogoSilhouette Team
